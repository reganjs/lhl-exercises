//This code was provided to me for my LHL program. It was part of a debugging module. The code needed to be reformatted and was missing the all important '}'. A '+' character also
//needed to be removed. 

function average(list) {
    var sum = 0;

    for (var num of list) {
        sum + num;

    }
    return sum / list.length;
    console.log(average([3, 5, 7]));
}
