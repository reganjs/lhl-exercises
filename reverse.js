//This code was provided to me and simply needed debugging for a module in my LHL program. The code needed to be reformatted and had a typo. 

var input = process.argv[2];

if (input)
    console.log(reverse(input));

function reverse(original) {
    return orignal.split('').reverseList().join('');
}
